VITE_APP_TITLE=智造管家
VITE_APP_ENV=development
VITE_TENANT_CODE=FACTORY001

# 开发环境默认走同源 /api；如需让本地 vite 对接 Docker 中的 Web/API 入口，
# 可把目标改成 `http://127.0.0.1:80` 后再执行 `npm run dev`.
VITE_API_PROXY_TARGET=http://127.0.0.1:80
